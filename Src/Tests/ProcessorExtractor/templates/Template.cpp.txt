#include "PLUGIN_$PROCESSOR_ID.h"

Q_EXPORT_PLUGIN2(PLUGIN_$PROCESSOR_ID, PLUGIN_$PROCESSOR_ID)

void PLUGIN_$PROCESSOR_ID::run()
{
    qDebug() << "Process";
    quint8 count = 0;
    while (count < 8) {
        qDebug() << "COUNT=" << count;
        msleep(2000);
        count++;
    }

}

void PLUGIN_$PROCESSOR_ID::init()
{
    // definition du processorId généré

    // definition du processorName
    _processorName = "Processeur de test";

    // definition des parameteres
    ProcessorParameter parameter1 = {"parametre 1", "Commentaire 1", "", "", INT, LINE_EDIT, 10, 0, 100, false};
    _parameters.append(parameter1);

}
